% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcovSpHAC.R
\name{vcovSpHAC.fixest}
\alias{vcovSpHAC.fixest}
\title{Spatial HAC variance-covariance matrix for fixest::feols models}
\usage{
\method{vcovSpHAC}{fixest}(
  reg,
  unit = NULL,
  time = NULL,
  lat,
  lon,
  kernel = c("bartlett", "uniform"),
  dist_fn = c("haversine", "spherical", "chord"),
  dist_cutoff = NULL,
  lag_cutoff = 0,
  verbose = FALSE,
  balanced_pnl = FALSE,
  ncores = NA,
  pixel = 0,
  neighbor = c("grid", "band"),
  csr_weight = c("double", "float"),
  data = NULL,
  ...
)
}
\arguments{
\item{reg}{A fitted object of class "fixest".}

\item{unit}{Optional name of the panel unit variable. If \code{NULL} the
call is treated as a cross-section (each row is its own unit, all rows
share a single period — no serial HAC).}

\item{time}{Optional name of the time variable. Ignored when \code{unit} is NULL.}

\item{lat}{Name of the latitude variable.}

\item{lon}{Name of the longitude variable.}

\item{kernel}{Spatial kernel, either "bartlett" or "uniform".}

\item{dist_fn}{Distance function, one of "haversine", "spherical", "chord".}

\item{dist_cutoff}{Spatial cutoff in km.}

\item{lag_cutoff}{Serial HAC lag cutoff.}

\item{verbose}{Print progress messages.}

\item{balanced_pnl}{Whether the panel is balanced and unit locations are time-invariant.}

\item{ncores}{Number of cores for the C++/RcppParallel spatial and serial routines.}

\item{pixel}{Score-pre-aggregation cell size, in kilometres.}

\item{neighbor}{Neighbor-search strategy: "grid" (default) or "band".
See \code{\link{vcovSpHAC.felm}}.}

\item{csr_weight}{Balanced-path bartlett weight storage: "double"
(default) or "float". See \code{\link{vcovSpHAC.felm}}.}

\item{data}{Optional. The model frame to draw \code{lat}/\code{lon}/
\code{unit}/\code{time} from. If \code{NULL} (default), the data is
recovered from the fit's call. Pass it explicitly if the original data
has gone out of scope, or if you want to override.}

\item{...}{Currently unused.}
}
\value{
A variance-covariance matrix.
}
\description{
Requires the fit to have been called with \code{feols(..., demeaned = TRUE)}
so that the centered design matrix \code{X_demeaned} is stored on the fit
object. Weighted fits are not supported.
}
