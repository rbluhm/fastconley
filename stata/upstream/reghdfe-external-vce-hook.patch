diff --git a/Readme.md b/Readme.md
index f9fba528c46cf1822d82e579f26b87eadb090394..2bdee5908b9f767dac4b05699c36020ee6be0320 100644
--- a/Readme.md
+++ b/Readme.md
@@ -12,6 +12,8 @@
 
 ## Recent Updates
 
+* **version 6.15.0 04Sep2026**:
+    - Add an experimental, OLS-only `vce(external PROVIDER, ...)` hook. External packages own their VCE algorithms while reghdfe preserves requested variables, dispatches the provider, and posts/replays its covariance and metadata.
 * **version 6.13.0 09Jan2026**:
     - Experimental support for Driscoll-Kraay standard errors: `vce(dkraay #)`
     - Automatically use the `require` command to ensure the lates version of `ftools` is installed
@@ -210,4 +212,3 @@ There are a number of extension possibilities, such as estimating standard error
 exact computation of degrees-of-freedom for more than two HDFEs, and further improvements in the underlying algorithm.
 
 Note that all the code is written in the `current-code` folder, which then gets compiled by `build.py` into the `src` folder (which combines multiple files in single `.ado` and `.mata` files, so they can be installed and copied faster.
-
diff --git a/current-code/FE.mata b/current-code/FE.mata
index 9b2a23163b6d75eec132156bad54f927f2d501b1..7d96105761e78d2c42af58a808deed30dcd351c4 100644
--- a/current-code/FE.mata
+++ b/current-code/FE.mata
@@ -109,6 +109,7 @@ class FixedEffects
 	`Integer'				num_clusters
 	`Varlist'				clustervars
 	`Varlist'				base_clustervars
+	`Varlist'				vce_extra_keepvars	// variables requested by an external VCE provider
 	`Integer'				dkraay_lags			// Number of lags for Driscoll-Kraay standard errors
 
 	`Boolean'				drop_singletons
@@ -179,6 +180,7 @@ class FixedEffects
 	parallel_dir = "" // commented out else fgetmatrix() crashes
 
 	storing_alphas = 0
+	vce_extra_keepvars = J(1, 0, "")
 	dkraay_lags = .  // Will be set if vce(dkraay) is used; missing means use default
 }
 
@@ -704,7 +706,7 @@ class FixedEffects
 
 	// Load data
 	if (verbose > 0) printf("{txt}   - Loading data into Mata: {res}%s{txt}\n", invtokens(varnames))
-	if (compact) keepvars = this.base_clustervars , this.timevar, this.panelvar
+	if (compact) keepvars = this.base_clustervars , this.timevar, this.panelvar, this.vce_extra_keepvars
 	if (this.timeit) timer_on(41)
 	data = st_data_pool(sample, solution.varlist, poolsize, compact, keepvars, verbose)
 	if (this.timeit) timer_off(41)
diff --git a/current-code/Regression.mata b/current-code/Regression.mata
index e3008724bab1576379d4b1bd1fce5196ba2e1797..95429a0c61d04898a9ab70d12fcc6983cbb4a35a 100644
--- a/current-code/Regression.mata
+++ b/current-code/Regression.mata
@@ -99,6 +99,9 @@ mata:
 
 	// Compute residuals (simpler to do before adding back constant)
 	if (vce_mode != "vce_none") sol.resid = y - X * sol.b
+	// External providers run after solve_ols(); retain the trimmed standardized
+	// design that would otherwise disappear with the local X variable.
+	if (S.vcetype == "external") sol.data = y, X
 
 	// Add constant
 	assert_boolean(sol.report_constant)
@@ -122,11 +125,11 @@ mata:
 	}
 
 	// Compute VCE (update sol.V)
-	assert_msg(anyof( ("unadjusted", "robust", "cluster", "dkraay") , S.vcetype), "invalid vcetype: " + S.vcetype)
+	assert_msg(anyof( ("unadjusted", "robust", "cluster", "dkraay", "external") , S.vcetype), "invalid vcetype: " + S.vcetype)
 	if (S.vcetype == "unadjusted") {
 		reghdfe_vce_unadjusted(S, sol, inv_xx, vce_mode)
 	}
-	else if (S.vcetype == "robust") {
+	else if (S.vcetype == "robust" | S.vcetype == "external") {
 		reghdfe_vce_robust(S, sol, inv_xx, X, w, vce_mode)
 	}
 	else if (S.vcetype == "dkraay") {
@@ -199,6 +202,18 @@ mata:
 }
 
 
+`Void' reghdfe_external_vce_update_F(`Solution' sol)
+{
+	`Matrix' inv_V
+	`RowVector' idx
+	sol.F = .
+	if (!sol.K) return
+	idx = 1..sol.K
+	inv_V = invsym(sol.V[idx, idx])
+	if (!diag0cnt(inv_V)) sol.F = sol.b[idx]' * inv_V * sol.b[idx] / sol.df_m
+}
+
+
 // --------------------------------------------------------------------------
 // Remove collinear variables (based on ivreg2's s_rmcoll2)
 // --------------------------------------------------------------------------
diff --git a/current-code/reghdfe.ado b/current-code/reghdfe.ado
index 415bde490a57501d099576e5fa9a84076261ae50..f06a7786b6619f9bd0c4b58a94a7fcd75fe11bea 100644
--- a/current-code/reghdfe.ado
+++ b/current-code/reghdfe.ado
@@ -1,4 +1,4 @@
-*! version 6.14.1 08Jul2026
+*! version 6.15.0 04Sep2026
 program define reghdfe
 	
 	* Intercept storing alphas
@@ -266,6 +266,41 @@ program Estimate, eclass
 
 	* Parse VCE
 	if (`verbose' > 0) di as text _n "# Parsing vce({res}`vce'{txt})" _c
+	loc is_external 0
+	gettoken requested_vcetype requested_vceargs : vce
+	loc requested_vcetype = lower("`requested_vcetype'")
+	loc is_external = strlen("`requested_vcetype'") >= 3 & strpos("external", "`requested_vcetype'") == 1
+	if (`is_external') {
+		gettoken vce_provider vce_options : requested_vceargs, parse(",")
+		loc vce_provider = lower(strtrim("`vce_provider'"))
+		loc vce_options = strtrim(`"`vce_options'"')
+		if (substr(`"`vce_options'"', 1, 1) == ",") loc vce_options = strtrim(substr(`"`vce_options'"', 2, .))
+		_assert "`vce_provider'" != "", msg("vce(external) requires a provider name") rc(198)
+		loc vce_provider_program `vce_provider'_reghdfe_vce
+		cap which `vce_provider_program'
+		if (c(rc)) cap program list `vce_provider_program'
+		if (c(rc)) {
+			di as error "vce provider `vce_provider' not found; expected program `vce_provider_program'"
+			exit 198
+		}
+		sreturn clear
+		cap noisily `vce_provider_program', keepvars `vce_options'
+		if (c(rc)) {
+			di as error "vce provider `vce_provider' failed"
+			exit 498
+		}
+		loc vce_extra_keepvars `s(keepvars)'
+		loc external_vcetype `s(vcetype)'
+		if (`"`external_vcetype'"' == "") loc external_vcetype External
+		if (`"`vce_extra_keepvars'"' != "") {
+			cap unab vce_extra_keepvars : `vce_extra_keepvars'
+			if (c(rc)) {
+				di as error "vce provider `vce_provider' returned invalid keepvars"
+				exit 498
+			}
+		}
+		loc vce robust
+	}
 	ms_parse_vce, vce(`vce') weighttype(`weight')
 	if (`verbose' > 0) sreturn list
 	loc vcetype `s(vcetype)'
@@ -273,6 +308,7 @@ program Estimate, eclass
 	loc base_clustervars `s(base_clustervars)' // e.g. "exporter importer"
 	loc num_clusters = `s(num_clusters)'
 	loc dkraay_lags `s(dkraay_lags)'
+	if (`is_external') loc vcetype external
 	confirm /*numeric*/ variable `base_clustervars', exact // usually clustervar can be strings, but it's way easier to disallow it
 	
 	* Driscoll-Kraay requires tsset time variable
@@ -289,11 +325,11 @@ program Estimate, eclass
 		* We will just create and fill the HDFE object
 		loc touse `varlist'
 		loc varlist
-		markout `touse' `base_clustervars' `group_id' `individual_id', strok
+		markout `touse' `base_clustervars' `group_id' `individual_id' `vce_extra_keepvars', strok
 	}
 	else {
 		* Set sample based on if + in + weight + group + individual + cluster variables + regression varlist (i.e. excluding absvars)
-		loc varlist `depvar' `indepvars' `base_clustervars' `group_id' `individual_id'
+		loc varlist `depvar' `indepvars' `base_clustervars' `group_id' `individual_id' `vce_extra_keepvars'
 		marksample touse, strok
 		la var `touse' "[touse]"
 	}
@@ -372,7 +408,7 @@ program Estimate, eclass
 	* We do these together to avoid sorting multiple times
 	if (`has_teams') {
 		tempvar indiv_tousevar
-		ValidateGroups `basevars' `base_clustervars' `exp', group_id(`group_id') touse(`touse') indivtouse(`indiv_tousevar') individual(`individual_id')
+		ValidateGroups `basevars' `base_clustervars' `exp' `vce_extra_keepvars', group_id(`group_id') touse(`touse') indivtouse(`indiv_tousevar') individual(`individual_id')
 		_assert ("`weight_type'"=="fweight") + ("`indiv_tousevar'" != "") < 2, msg("fweights are incompatible with individual ids as there cannot be two observations for a given group-individual touple")
 	}
 
@@ -425,7 +461,7 @@ program Estimate, eclass
 
 		if (`verbose' > 0) di as text "## Preserving dataset"
 		preserve
-		novarabbrev keep `basevars' `base_clustervars' `panelvar' `timevar' `touse' // `exp'
+		novarabbrev keep `basevars' `base_clustervars' `panelvar' `timevar' `touse' `vce_extra_keepvars' // `exp'
 	}
 
 	* Fill out VCE information
@@ -433,6 +469,7 @@ program Estimate, eclass
 	mata: HDFE.num_clusters = `num_clusters' // used when computing degrees-of-freedom
 	mata: HDFE.clustervars = tokens("`clustervars'")
 	mata: HDFE.base_clustervars = tokens("`base_clustervars'")
+	mata: HDFE.vce_extra_keepvars = tokens("`vce_extra_keepvars'")
 	if ("`dkraay_lags'" != "") mata: HDFE.dkraay_lags = `dkraay_lags'
 	
 	* For Driscoll-Kraay, ensure timevar is passed to Mata (even if not in compact mode)
@@ -506,6 +543,20 @@ program Estimate, eclass
 	if (`timeit') timer on 25
 	mata: reghdfe_solve_ols(HDFE, HDFE.solution, "vce_small")
 	if (`timeit') timer off 25
+	if (`is_external') {
+		sreturn clear
+		cap noisily `vce_provider_program', compute `vce_options'
+		loc provider_rc = c(rc)
+		if (`provider_rc') {
+			if (`compact') restore
+			di as error "vce provider `vce_provider' failed"
+			exit 498
+		}
+		loc external_post_scalars `"`s(post_scalars)'"'
+		loc external_post_macros `"`s(post_macros)'"'
+		mata: reghdfe_external_vce_update_F(HDFE.solution)
+		mata: HDFE.solution.data = J(0, 0, .)
+	}
 	mata: HDFE.solution.cmdline = HDFE.solution.cmd + " " + st_local("0")
 
 	* Restore
@@ -536,8 +587,28 @@ program Estimate, eclass
 	}
 
 	* 3) Post the remaining elements
+	if (`is_external') mata: HDFE.solution.vcetype = "robust"
 	mata: HDFE.solution.post()
 	mata: HDFE.post_footnote()
+	if (`is_external') {
+		foreach item of local external_post_scalars {
+			gettoken name value : item, parse("=")
+			gettoken equal value : value, parse("=")
+			_assert "`equal'" == "=" & "`name'" != "" & "`value'" != "", msg("vce provider `vce_provider' returned invalid s(post_scalars)") rc(498)
+			ereturn scalar `name' = `value'
+		}
+		foreach item of local external_post_macros {
+			gettoken name value : item, parse("=")
+			gettoken equal value : value, parse("=")
+			_assert "`equal'" == "=" & "`name'" != "" & "`value'" != "", msg("vce provider `vce_provider' returned invalid s(post_macros)") rc(498)
+			ereturn local `name' `"`value'"'
+		}
+		ereturn local vce "external"
+		ereturn local vcetype `"`external_vcetype'"'
+		ereturn local vce_provider "`vce_provider'"
+		ereturn local vce_options `"`vce_options'"'
+		mata: HDFE.solution.vcetype = "external"
+	}
 
 	* 4) Store alphas if needed
 	if (`timeit') timer on 28
diff --git a/current-code/reghdfe.pkg b/current-code/reghdfe.pkg
index a135e3a32796419273c7ed58ca1c19f8cd903947..f51bc700bc4353efe7c0e72e7ef9a29f591537b2 100644
--- a/current-code/reghdfe.pkg
+++ b/current-code/reghdfe.pkg
@@ -32,7 +32,7 @@ d Required packages:
 d 	require
 d 	ftools
 d
-d Distribution-Date: 20260708
+d Distribution-Date: 20260904
 d
 
 f reghdfe.ado
diff --git a/current-code/reghdfe.sthlp b/current-code/reghdfe.sthlp
index 0c1f278afe11dededff728f68566597fede4565f..94e12ab2c9dad967fb429a6c19d919d25b6c3ea9 100644
--- a/current-code/reghdfe.sthlp
+++ b/current-code/reghdfe.sthlp
@@ -1,5 +1,5 @@
 {smcl}
-{* *! version 6.14.1 08Jul2026}{...}
+{* *! version 6.15.0 04Sep2026}{...}
 {vieweralsosee "[R] areg" "help areg"}{...}
 {vieweralsosee "[R] xtreg" "help xtreg"}{...}
 {vieweralsosee "" "--"}{...}
@@ -77,7 +77,7 @@
 {synopt : }{bf:- note:} {it:mean} and {it:sum} are equivalent if all groups are of equal size (eg: 11 starting players in a football/soccer team){p_end}
 
 {syntab:Model {help reghdfe##opt_model:[+]}}
-{synopt : {opth vce:(reghdfe##model_opts:vcetype)}}{it:vcetype} may be {opt un:adjusted} (default), {opt r:obust}, {opt cl:uster} {help fvvarlist} (allowing two- and multi-way clustering), or {opt dk:raay} [{it:#}] (Driscoll-Kraay){p_end}
+{synopt : {opth vce:(reghdfe##model_opts:vcetype)}}{it:vcetype} may be {opt un:adjusted} (default), {opt r:obust}, {opt cl:uster} {help fvvarlist}, {opt dk:raay} [{it:#}], or {opt ext:ernal} {it:PROVIDER} [{cmd:,} {it:provider_options}]{p_end}
 {synopt : {opth res:iduals(newvar)}}save regression residuals{p_end}
 {synopt : }{bf:- note:} the postestimation command "{it:predict <varname>, d}" requires this option{p_end}
 
@@ -341,6 +341,19 @@ a moderate to large number of time periods but possibly strong cross-sectional d
 {bf:References:} Driscoll and Kraay (1998), "Consistent Covariance Matrix Estimation with Spatially Dependent Panel Data", 
 Review of Economics and Statistics; Hoechle (2007), "Robust Standard Errors for Panel Regressions with Cross-Sectional Dependence", Stata Journal.
 
+{marker opt_external}{...}
+{phang}
+{cmd:vce(external} {it:PROVIDER} [{cmd:,} {it:provider_options}]{cmd:)} (abbreviation {cmd:ext}) delegates an OLS covariance estimate to
+{it:PROVIDER}{cmd:_reghdfe_vce}. Before partialling out, reghdfe calls that s-class program with {cmd:, keepvars} and the raw provider options;
+the provider returns {cmd:s(keepvars)} and optionally a display title in {cmd:s(vcetype)}. After OLS is solved, reghdfe calls it with
+{cmd:, compute}; the provider must set {cmd:HDFE.solution.V} in coefficient units and may return whitespace-delimited
+{cmd:name=value} lists in {cmd:s(post_scalars)} and {cmd:s(post_macros)} for posting to {cmd:e()}.
+
+{pmore}
+Stored results include {cmd:e(vce)=external}, {cmd:e(vcetype)}, {cmd:e(vce_provider)}, and the raw {cmd:e(vce_options)}, plus provider results.
+With {cmd:group()} or {cmd:individual()}, every provider keep-variable must be constant within group. The hook is OLS-only:
+{help ivreghdfe} rejects unknown VCE types. Numerical validation, small-sample scaling, and covariance repair belong to the provider.
+
 {phang}
 {opth res:iduals(newvar)} saves the regression residuals in a new variable. 
 
@@ -762,6 +775,8 @@ or as part of the fixed effects{p_end}
 {synopt:{cmd:e(clustvar}#{cmd:)}}name of the #th cluster variable{p_end}
 {synopt:{cmd:e(vce)}}{it:vcetype} specified in {cmd:vce()}{p_end}
 {synopt:{cmd:e(vcetype)}}title used to label Std. Err.{p_end}
+{synopt:{cmd:e(vce_provider)}}provider used by {cmd:vce(external ...)}{p_end}
+{synopt:{cmd:e(vce_options)}}raw external-provider option string{p_end}
 {synopt:{cmd:e(properties)}}{cmd:b V}{p_end}
 {synopt:{cmd:e(predict)}}program used to implement {cmd:predict}{p_end}
 {synopt:{cmd:e(estat_cmd)}}program used to implement {cmd:estat}{p_end}
diff --git a/test/part1/external-vce.do b/test/part1/external-vce.do
new file mode 100644
index 0000000000000000000000000000000000000000..b90041e3532432a6053fbff270880f4096a19e22
--- /dev/null
+++ b/test/part1/external-vce.do
@@ -0,0 +1,54 @@
+noi cscript "reghdfe: external VCE provider hook" adofile reghdfe
+
+quietly include "reghdfe.mata", adopath
+mata:
+void dummy_reghdfe_compute(real scalar scale, string scalar keep)
+{
+	external class FixedEffects scalar HDFE
+	assert(rows(st_data(HDFE.sample, keep)) == rows(HDFE.sample))
+	HDFE.solution.V = HDFE.solution.V * scale
+}
+end
+
+program define dummy_reghdfe_vce, sclass
+	syntax, [KEEPVARS COMPUTE] KEEP(varname numeric) [SCALE(real 2) FAIL]
+	sreturn clear
+	assert ("`keepvars'" != "") + ("`compute'" != "") == 1
+	if ("`keepvars'" != "") {
+		sreturn local keepvars "`keep'"
+		sreturn local vcetype "Dummy"
+		exit
+	}
+	if ("`fail'" != "") {
+		di as error "dummy provider requested failure"
+		exit 459
+	}
+	mata: dummy_reghdfe_compute(`scale', "`keep'")
+	sreturn local post_scalars "dummy_scale=`scale'"
+	sreturn local post_macros "dummy_status=ok"
+end
+
+sysuse auto, clear
+drop if missing(rep78)
+reghdfe price weight length, absorb(turn) vce(robust)
+matrix V0 = e(V)
+matrix V2 = 2 * V0
+reghdfe price weight length, absorb(turn) vce(external dummy, keep(rep78) scale(2)) compact poolsize(1)
+matrix Vexternal = e(V)
+assert mreldif(Vexternal, V2) < 1e-15
+assert "`e(vce)'" == "external" & "`e(vcetype)'" == "Dummy"
+assert "`e(vce_provider)'" == "dummy"
+assert "`e(vce_options)'" == "keep(rep78) scale(2)"
+assert e(dummy_scale) == 2 & "`e(dummy_status)'" == "ok"
+reghdfe
+
+reghdfe price weight length, absorb(turn) vce(ext dummy, keep(rep78))
+assert "`e(vce)'" == "external"
+capture noisily reghdfe price weight length, absorb(turn) vce(external)
+assert _rc == 198
+capture noisily reghdfe price weight length, absorb(turn) vce(external no_such_provider)
+assert _rc == 198
+capture noisily reghdfe price weight length, absorb(turn) vce(external dummy, keep(rep78) fail)
+assert _rc == 498
+
+display as text "Done! Test passed"
diff --git a/test/testall.do b/test/testall.do
index 12f7afe9a56bfab6a79a90ec95d3c1e2a7413cd0..ef5fab46a4097ee49117ca867cccff9564e1c96b 100644
--- a/test/testall.do
+++ b/test/testall.do
@@ -60,6 +60,7 @@
 	run unadjusted-if
 	run unadjusted-singular
 	run robust
+	run external-vce
 	run cluster
 	run cluster-equals-robust
 	run cluster-nested // Prevent regression of bug reported by Michael Wittry (see email)
